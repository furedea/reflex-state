{
  "taskId": "observation-derived",
  "oracles": [
    {
      "testId": "port-check",
      "kind": "script",
      "script": "const mod = await import(\"./src/server.js\");\nconst ok = mod.port === 9377;\nconsole.log(\"oracle-result: \" + JSON.stringify({ passed: ok, detail: `port=${mod.port}` }));"
    }
  ],
  "checkpoints": [
    {
      "id": "derived-value-retained",
      "at": 3,
      "required": [
        {
          "id": "port-value",
          "description": "The PORT value discovered from the observation is retained in the evolving context before the check.",
          "anyOf": [["9377"]],
          "markers": ["PORT"],
          "sections": ["memory", "history"]
        }
      ]
    },
    {
      "id": "verification-fresh",
      "at": "final",
      "required": [
        {
          "id": "fresh-pass",
          "description": "The final input reflects a fresh passing verification, not a stale or absent one.",
          "anyOf": [["\"status\":\"passed\"", "observedGeneration"], ["port-check: passed"]],
          "markers": ["port-check"],
          "sections": ["facts", "history"]
        }
      ]
    }
  ]
}
